# 文件管理功能优化任务计划

## 问题清单

| # | 问题 | 严重程度 | 状态 |
|---|------|----------|------|
| 1 | DB配置Key不匹配，MinIO配置无法正确读取 | 🔴 严重 | 待处理 |
| 2 | 前端存储子分组判断Key不匹配，设置页面显示异常 | 🔴 严重 | 待处理 |
| 3 | FileAsset不记录存储类型，切换存储后旧文件无法访问 | 🟡 中等 | 待处理 |
| 4 | 前端上传进度100%但后端还在处理 | 🟡 中等 | 待处理 |
| 5 | 秒传检查在切换存储后失效 | 🟡 中等 | 待处理 |
| 6 | 删除文件不删除物理文件 | 🟡 中等 | 待处理 |
| 7 | 分享链接硬编码IP | 🟢 低 | 待处理 |

## 任务列表

### 任务1: 修复DB配置Key不匹配问题
- 修改 `manager.go` 的 `loadStorageConfigFromDB()` 使用正确的key
- 验证MinIO配置能正确加载

### 任务2: 修复前端存储设置页面
- 修改 `settings/index.vue` 的 `getStorageSubGroup()` 使用正确的key前缀
- 确保选择MinIO时显示对应的配置项

### 任务3: FileAsset添加storage_type字段
- 修改 `file_asset.go` 模型添加字段
- 创建数据库迁移
- 修改上传服务记录存储类型
- 修改文件访问服务根据存储类型选择正确的存储实例

### 任务4: 优化前端上传体验
- 显示"正在保存到服务器..."提示
- 后端complete接口返回后再显示100%

### 任务5: 修复秒传检查
- 检查时验证文件是否在当前存储中实际存在

### 任务6: 删除文件时清理物理存储
- 引用计数为0时删除物理文件

### 任务7: 修复分享链接硬编码
- 使用动态域名

## 执行顺序

1 → 2 → 3 → 4 → 5 → 6 → 7
